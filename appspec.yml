version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/ggwp
    overwrite: yes

permissions:
  - object: /home/ubuntu/ggwp
    pattern: "**"
    onwer: ubuntu
    group: ubuntu

hooks:
  ApplicationStart:
    - location: scripts/gh_bdeploy.sh
      timeout: 60
      runas: ubuntu